# 仓库根即引擎包（name: ai-fly 不动），webui 为独立 workspace 成员（A1）。
packages:
  - .
  - webui
allowBuilds:
  esbuild: true
  # sharp 0.33 经 @img/* optional 依赖携带预编译二进制，安装脚本无需执行
  sharp: false
onlyBuiltDependencies:
  - esbuild
